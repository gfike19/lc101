def add_thou(text):
    new_text = ""
    for word in text:
        if word == "the":
            new_text = new_text + "thou"
        else:
            new_text = new_text + word'
    return new_text

def get_speaker(text):
    thou_count = 0
    for word in text:
        if wourd == "the":
            count = count + 1
   
   if thou_count <

def make_dec(thou_count,)